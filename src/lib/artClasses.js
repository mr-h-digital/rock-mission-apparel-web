// Explicit Tailwind class names (never build these via template literals) so
// the content scanner can find and keep them in the production build.
export const ART_CLASSES = {
  'grad-drop': 'bg-grad-drop',
  'grad-volt': 'bg-grad-volt',
  'grad-fire': 'bg-grad-fire',
}
